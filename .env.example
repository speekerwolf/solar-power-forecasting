# Copy to `.env` (never commit `.env`). See README for NSRDB key steps.
# NREL Developer Network API key (used for NSRDB and other NREL APIs)
NREL_API_KEY=your_key_here

# Optional: default PVDAQ system id after you run facility selection (integer)
# SELECTED_SYSTEM_ID=1234
