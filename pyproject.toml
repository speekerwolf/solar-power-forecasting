[project]
name = "solar-power-forecasting"
version = "0.1.0"
description = "Solar PV output forecasting using PVDAQ + NSRDB (portfolio project)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.0",
  "numpy>=1.24",
  "matplotlib>=3.7",
  "seaborn>=0.13",
  "jupyter>=1.0",
  "ipykernel>=6.0",
  "scikit-learn>=1.3",
  "xgboost>=2.0",
  "requests>=2.31",
  "python-dotenv>=1.0",
]

[tool.uv]
package = false

